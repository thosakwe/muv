function myFunction(x:number) {
  console.info(x);
}

myFunction(2.0);

// Destructuring parameters!!!
function printTodo({text:string, completed:boolean}):void {
  console.log(text, ":", completed);
}

printTodo({text: 'Clean your room!', completed: false});